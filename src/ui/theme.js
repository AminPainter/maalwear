import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#dba13a',
    },

    secondary: {
      main: '#3d8d71',
    },
  },

  borderRadius: {
    tiny: '3px',
  },
});

export default theme;
